print("Working fine...")
print("Jay Ganesh")
'''
- python -m compileall    -- Generates pycache folder with compiled files of curr dir. -m is module of python.
- python version is 3.10.7 and implemention is cpython
- .py file gets converted to .pyc
- There is no entry point function in python unlike c, c++, java etc.
    So we create our own entry point function for python.
- The first executable line writted at indent(0) will be the execution starting point.
- If a variable has __name__ then it is a special variable which tells from main module the code is running. 
- In python all variables are called data objects.
- The variable name and its value assigned is called as data object or data variable.
'''